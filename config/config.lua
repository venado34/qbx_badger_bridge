Config = {}

Config.Debug = true

-- Options: 'crm-hud', 'ox_lib'
Config.NotificationSystem = 'crm-hud'

-- Configure the commands
Config.Commands = {
    jobsMenu = "jobsmenu",
    syncJobs = "syncjobs"
}

-- The ACE permission required to use administrative commands like /syncjobs.
Config.AdminPermission = "admin"
